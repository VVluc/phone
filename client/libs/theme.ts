import { createTheme } from '@nextui-org/react';

export const theme = createTheme({
  type: 'light', // light / dark
  theme: {
    colors: {
      secondary: '#bd1c0e',
      ghostWhite: '#fdfdff',
    },
  },
});
